name: Release Desktop EXE

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  attestations: write
  id-token: write

jobs:
  release-windows-exe:
    runs-on: windows-latest
    env:
      WINDOWS_CERT_BASE64: ${{ secrets.WINDOWS_CERT_BASE64 }}
      WINDOWS_CERT_PASSWORD: ${{ secrets.WINDOWS_CERT_PASSWORD }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop

      - name: Build release binary
        run: cargo build --release
        working-directory: apps/desktop

      - name: Sign executable (optional)
        if: ${{ env.WINDOWS_CERT_BASE64 != '' && env.WINDOWS_CERT_PASSWORD != '' }}
        continue-on-error: true
        shell: pwsh
        run: |
          $exe = Get-ChildItem apps/desktop/target/release/*.exe | Select-Object -First 1
          if (-not $exe) { throw "No executable found to sign." }
          $signtool = (Get-Command signtool.exe -ErrorAction SilentlyContinue).Source
          if (-not $signtool) {
            $signtool = Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\bin\*\x64\signtool.exe" -ErrorAction SilentlyContinue |
              Sort-Object FullName -Descending |
              Select-Object -First 1 -ExpandProperty FullName
          }
          if (-not $signtool) {
            Write-Warning "signtool.exe not found on runner. Skipping signing."
            exit 0
          }
          $certPath = Join-Path $env:RUNNER_TEMP "codesign.pfx"
          [IO.File]::WriteAllBytes($certPath, [Convert]::FromBase64String($env:WINDOWS_CERT_BASE64))
          & $signtool sign /fd SHA256 /f $certPath /p $env:WINDOWS_CERT_PASSWORD /tr http://timestamp.digicert.com /td SHA256 $exe.FullName
          if ($LASTEXITCODE -ne 0) {
            Write-Warning "Signing failed with exit code $LASTEXITCODE. Continuing with unsigned binary."
          }

      - name: Generate checksums
        shell: pwsh
        run: |
          $out = "apps/desktop/target/release/SHA256SUMS.txt"
          $lines = Get-ChildItem apps/desktop/target/release/*.exe | ForEach-Object {
            $hash = (Get-FileHash $_.FullName -Algorithm SHA256).Hash.ToLower()
            "$hash *$($_.Name)"
          }
          $lines | Set-Content -Path $out -Encoding ascii

      - name: Attest release artifacts
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: |
            apps/desktop/target/release/*.exe
            apps/desktop/target/release/SHA256SUMS.txt

      - name: Publish release artifacts
        uses: softprops/action-gh-release@v3
        with:
          files: |
            apps/desktop/target/release/*.exe
            apps/desktop/target/release/SHA256SUMS.txt

